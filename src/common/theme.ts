import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      light: '#8ca7ce',
      main: '#f07b61',
      dark: '#002e4d',
      contrastText: '#ffffff',
    },
  },
})
